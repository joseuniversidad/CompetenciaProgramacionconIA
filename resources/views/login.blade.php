<!doctype html><html lang="es"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Acceso · Solar GT</title><link rel="stylesheet" href="/app.css"><body class="login"><form method="post" action="/login" id="login-form" class="card login-card">@csrf<div class="brand">☀ Solar<span>GT</span></div><h1>Administrar la energía</h1><p>Ingresa para registrar granjas, paneles y generación.</p>@if($errors->any())<div class="error" role="alert" id="login-error">{{ $errors->first() }}</div>@endif<label>Correo electrónico<input type="email" name="email" value="{{ old('email') }}" required autocomplete="username"></label><label>Contraseña<input type="password" name="password" required autocomplete="current-password"></label><button class="primary">Iniciar sesión</button><a href="/">← Volver al dashboard público</a></form><script src="/vendor/gsap/gsap.min.js"></script><script>
if (window.gsap) {
    gsap.from('#login-form', { opacity: 0, y: 26, scale: .97, duration: .55, ease: 'power2.out' });
    const err = document.getElementById('login-error');
    if (err) gsap.fromTo(err, { x: 0 }, { x: 8, duration: .06, repeat: 5, yoyo: true, delay: .5, ease: 'power1.inOut', clearProps: 'x' });
}
</script></body></html>